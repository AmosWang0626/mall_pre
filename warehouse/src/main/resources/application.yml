# 基础配置
spring:
  application:
    name: mall-warehouse
server:
  port: 8093

info:
  app:
    name: ${spring.application.name}
    description: 仓储系统
    version: 1.0.0

# eureka 服务注册中心配置
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
